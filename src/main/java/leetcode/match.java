package leetcode;

import java.util.Arrays;
import java.util.List;

/**
 * @description: '.' 匹配任意单个字符
 *               '*' 匹配零个或多个前面的那一个元素
 * @author: xfh
 * @create: 2019-06-25 14:25
 **/

public class match {
    /*public boolean isMatch(String s, String p) {
        List<String> ss = Arrays.asList(s);
        List<String> pp = Arrays.asList(p);
        for(int i = 0;i < s.length();i++){
            for(int j = 0;j<p.length();j++){
                if(!ss.get(i).equals(pp.get(j)) && !pp.get(j).equals("*")){
                    continue;
                }else {

                }
            }
        }
    }*/
}
